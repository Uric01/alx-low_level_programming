#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char *pt = "_putchar";

	while (*pt)
	{
		_putchar(*sh);
		sh++;
	}
	_putchar('\n');

	return (0);
}
